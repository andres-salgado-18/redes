#### Ejercicio 10
**a)** 
- Request al resolver `dns-1` para obtener el IP del SMTP saliente
- Para `uba.ar` se hace un request tipo MX, una vez obtene la IP del mail server puede enviar los primeros 3 mails
- Para `dc.uba.ar, dm.uba.ar, df.uba.ar` hace 3 request DNS para sus IPs a su resolver
Con lo cual serán 
- 1 consulta para el SMTP
- 2 consultas para cada dominio, esto debido a que MX devolverá el servidor que recibe mails y se debe hacer una consulta A para obtener la IP del host indicado por MX
- Es decir unas 9 consultas DNS

**b)**
```
GET /ofertas.png HTTP/1.1
Host: www.unaempresadespam.com.ar

200 OK
```
```
GET /count.php?CampaignID=10057&Email=[[Mail]] HTTP/1.1
Host: tracker.unaempresadespam.com.ar

200 OK
```

